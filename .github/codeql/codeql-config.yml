name: "CodeQL config"

paths-ignore:
  # Vite-bundled build artifact (vendors lit-html); not authored source.
  # Third-party dependency vulnerabilities are tracked separately through
  # GitHub dependency alerts and Dependabot updates.
  - custom_components/codex_usage/frontend/codex-usage-card.js
