# Block 1 — consistent data and card

Goal: reconcile source values once and present them consistently in Home Assistant and the card.
Architecture/tech stack/global constraints: [execution plan](2026-09-05-v0.7-implementation.md); binding [specification](../specs/2026-09-05-v0.7-design.md).
Execute master Tasks 1 and 2 with Subagent-Driven Development and TDD. Their checklists are the exact implementation steps and file ownership.

Order: strict parsing -> pure summaries -> coordinator live states/context -> entity/serializer consumers -> frontend parser/viewmodel -> renderer/editor -> actual browser checks.
Shared contract: schema1 additions optional, unknown nullable, affected statuses include windowless restrictions, reset metadata from one summary. Preserve identity and user configuration.
Checkpoint: Python and frontend regressions, generated bundle, compatibility fixtures and responsive rendering pass. Actual recorder runtime migration remains a required gate in Block3. Local commits only; version still0.6.5.
