# Codex Usage 0.7 implementation plan

**Goal:** Deliver the approved 0.7 candidate as one locally committed release candidate, without publication.
**Architecture:** Existing API/coordinator with pure monitoring, retry and budget helpers; additive schema-1 card transport; existing registry identities.
**Tech stack:** Python 3.14, Home Assistant 2026.3 minimum / 2026.8.3 validation target, Lit, TypeScript, Vite, pytest, Vitest, Playwright.
**Spec:** `docs/superpowers/specs/2026-09-05-v0.7-design.md` is binding, including its complete acceptance matrix.
**Execution:** Use superpowers:subagent-driven-development and test-driven-development. Finish each task with a spec/quality review. Review the entire candidate before reporting completion.

## Global constraints

- Work only in `D:/Projekte/Coding/Codex_Usage/.worktrees/v0.7`, branch `codex/v0.7`. Baseline `8e0a250` (application baseline 0.6.5). Local commits authorized; no push, merge, tag, release, purchases, reset actions, external notifications or production credentials.
- Preserve IDs, names, workspace/user identity, read permissions, old card configuration and schema version 1. All payload additions are optional, allowlisted and bounded. Unknown is not zero or false.
- Documentation/Blueprint English; UI English and German. Sources hidden by default, details collapsed, optional sections Automatic, explicit hides retained.
- TDD for behavior: write a focused regression test, observe meaningful RED, implement, observe GREEN. Keep evidence in task report. Run broader suite only at a checkpoint or to resolve concrete risk.
- Python command uses `D:/Projekte/Coding/Codex_Usage/.venv/Scripts/python.exe`; execute from worktree. Frontend commands use `npm.cmd` in its frontend directory.
- Commit scoped files only. Generated bundle belongs with each frontend change. Version remains 0.6.5 until final preparation task. No dependency upgrades unrelated to a required gate.
- Linux runtime, HACS/hassfest and CodeQL results must be reported honestly; unavailable infrastructure is not a pass and must not prevent preparing all tests and finishing local implementation.

## Delivery order and interfaces

| Block | Tasks | Checkpoint |
| --- | --- | --- |
| [1: consistent data and card](2026-09-05-v0.7-block-1.md) | 1, 2 | Backend + card + bundle, old/new compatibility and relevant tests |
| [2: robust operation](2026-09-05-v0.7-block-2.md) | 3 | Options, retry, credentials, discovery, Repairs |
| [3: optional user functions and delivery](2026-09-05-v0.7-block-3.md) | 4, 5 | Budget/timestamps, Blueprint, docs, screenshots, runtime matrix, aligned 0.7 candidate |

Task 1 establishes shared source/reset/status DTOs and allowlisted transport. Task 2 consumes those additions without requiring them. Task 3 owns HTTP scheduling and enriches live source states. Task 4 uses the established source timestamps and adds canonical per-window budgets (not client-derived budgets). Task 5 validates actual interfaces and records publication gates. Implementers report public helper names and transport fields for the next task. Code ownership is serial between implementation tasks.

### Task 1: Normalize monitoring data and backend consumers

Read the binding spec sections 3, 4 and 6 plus backend-relevant acceptance rows in section 11. Implement all backend consistency rules, leaving robust request controls to Task 3 and new timestamp/budget entities to Task 4.

Files: modify `api.py`, `coordinator.py`, `sensor.py`, `binary_sensor.py`, `card_data.py`, `diagnostics.py`, UI entity translations; create `monitoring.py`; extend `tests/test_api.py`, `test_entities.py`, `test_coordinator.py`, `test_card_data.py`, `test_diagnostics.py`; create `tests/test_monitoring.py`. All component paths are under `custom_components/codex_usage/`.

- [ ] RED: empty/malformed credit flags and reset count are None; explicit zero/false survive; absent/null/non-list/list reset details preserve presence independently of parsed rows and counts.
- [ ] Add SourceState (four source keys, safe enum metadata, explicit clock), LimitStatus and aggregate restriction, ResetSummary pure normalization. Coordinator owns live sources separately from last successful data. Optional payload failures retain session values, success missing fields clears them. Prepare defaults compatible with later options. Entity and card summaries must share the same clock/context.
- [ ] RED/implement full limit precedence: explicit restriction wins, false only if main/all reported features known unrestricted, optional absent credits do not create unknown; named windowless restrictions survive. Deduplicate main aliases. Detect/counter duplicate IDs; conflicting numeric windows become unknown, restrictive status survives. Bounded sorted affected IDs and safe counters, no raw diagnostic payloads.
- [ ] RED/implement reset priority: usage 4/detail 1 ->4 with no expiry; usage 0 wins; unknown falls back only to enabled details <=2h; missing all ->None; earliest future explicitly available expiry requires positive matching count, freshness and reconciled context. Historical lifetime total separately retained with age.
- [ ] Store last detail-reconciled usage context and changed-at marker in coordinator. Count changes or main reset shifts >60s invalidate old expiry until detail fetch catches up; no extra requests. Compare against reconciled context so accumulated jitter is bounded. Never infer redemption, local decrement, expiry duration or new weekly date.
- [ ] Update aggregate binary attrs and nullable credit binaries. Fix right-suffix dynamic registry recovery including `_primary_` inside IDs and future `_budget`; translated dynamic names with neutral duration placeholders. Preserve identities and user names.
- [ ] Correct profile state-class targets per spec and add metadata tests; actual minimum recorder runtime gate belongs to Task 5. Preserve measured percentage/pace classes and current credit-class absence.
- [ ] Add schema-1 `limit_statuses`, `sources`, reset provenance/consistency/detail presence; keep legacy fields/types. Unknown reset payload with no useful detail is null. Core outage uses live source states, keeps last-known data and permissions fail closed. New diagnostics contain only enumerated states/counters/options, never unrecognized names/values.
- [ ] GREEN: run focused tests, then entire Python suite and Ruff format/check once. Inspect diff. Commit `feat: normalize usage monitoring and reset consistency` and report helper interfaces, test evidence and any unresolved runtime gate.

### Task 2: Implement layout A and compatible card editor

Read spec section 5 and card-relevant sections 4/11. Consume Task 1's actual serializer; never assume absent new fields exist. Budget rendering supports optional future canonical fields but Task 4 supplies them.

Files: frontend `src/types.ts`, `card-data.ts`, `config.ts`, `status.ts`, `view-model.ts`, `format.ts`, `localize.ts`, `codex-usage-card.ts`, new `codex-usage-card-editor.ts`, `index.ts` as needed, existing `tests/` and `visual-tests/`, `visual-harness.ts`; generated component frontend bundle.

- [ ] RED: old/new schema1 snapshot parsing, nullable statuses, windowless feature restriction does not color healthy main windows, signed/zero credit balances with false availability, percentage-only spend, no-data remains neutral.
- [ ] Add strict optional allowlists and types for statuses/sources/reset metadata/budget. Preserve old parser compatibility fixture (use actual baseline normalization behavior). Main slots five-hour/weekly always retained unless explicitly hidden; nonstandard windows shown too.
- [ ] RED/implement layout A: account summary/main rows, named restriction callout, compact Available resets, collapsible additional/credits/spend/profile/account details, main freshness, optional source list. One em dash for forced-empty optional section; auto hides empty values; false and per-value false win. No fake zero progress or aria-valuenow on unknown. Restriction text does not claim all execution stopped.
- [ ] Remove currency formatting for all credit/spend quantities. Preserve signed finite balance independently of flags; unlimited separately. Historical profile group marker even with source list hidden. Sources auto only actionable polling issues; disabled/unsupported/discovery age not warnings.
- [ ] Add budget section auto and sources false defaults. Split editor retaining custom element registration and saved settings/HA layout fields; real Automatic/Always show/Hide selectors instead of boolean toggles. UI labels English/German.
- [ ] Add connected-only minute timer with cleanup/reconnection tests, no timer networking. Suppress expired or reset-imminent (<1min) budgets locally; never independently calculate canonical budget. Card converts pp/h to pp/day only for full duration >=1day.
- [ ] Verify responsive/theme/keyboard/escaped-label fixtures using synthetic Code review examples, existing account selection behavior and More Info permission/enablement. Update old rendering tests to new intended behavior, not weaken assertions.
- [ ] Run frontend format, lint, typecheck, Vitest/coverage, build and Playwright. Commit bundle with source (`feat: refresh usage card and visibility controls`). Record any browser/runtime limitation explicitly.

### Task 3: Make polling, authentication and lifecycle robust

Read spec section 7 and SourceState contract. Preserve Task 1 interfaces and Task 2 frontend assumptions. No new endpoint or authentication system.

Files: new `retry.py`, `repairs.py`; modify `api.py`, `coordinator.py`, `config_flow.py`, `const.py`, `__init__.py`, card registration helper as required, `diagnostics.py`, `strings.json`, `translations/en.json`, `translations/de.json`; corresponding unit tests.

- [ ] RED/implement Retry-After integer/date, valid server Date delta, invalid/past/zero floor, very long deadline without shortening. Typed safe request error with HTTP status/deadline; parse on 429 and 503. Never expose body/header text through diagnostics/errors.
- [ ] Entry-wide 429 cooldown stops subsequent optional reads same cycle and manual retries; 503 endpoint-only cooldown; ordinary optional retry900s, success/unsupported3600s, main60..3600 default300. Keep full deadline while bounded coordinator scheduling wakeups; no sleeps or concurrent token races. Source last_attempt advances only actual request, success only successful response.
- [ ] RED token rotation then resource429/503/malformed/connection failure: successful fresh tokens must persist before request through explicit credential callback or separate refresh; preserve discovery metadata and immutable identity. Avoid double-save on normal success. Failed optional auth uses existing behavior safely.
- [ ] Integration options fetch_profile/fetch_reset_details default true for old/new entries, disable skips HTTP and payload; usage-sourced reset count survives; reload listener works. Source disabled/unsupported distinct from error.
- [ ] Capture actual workspace discovery success timestamp during setup/reauth only; persist optional metadata, old entries unknown, preserve on token rotation. Reject both workspace and user identity change on reauth before updating credentials. No periodic accounts/check or reconfigure.
- [ ] One integration-wide Repair for storage-mode automatic card registration failure, dedup across entries; clear after success/last unload; YAML no warning. English/German issue text. Keep first refresh before registration.
- [ ] Safe diagnostics expose options/source deadline counters without raw provider identifiers. Cover optional failure/core availability and source state after core failure.
- [ ] Run focused RED/GREEN, whole Python suite, Ruff. Commit `feat: harden polling and authentication lifecycle` with report of exact cooldown/error interfaces and verified tests.

### Task 4: Add optional budgets, timestamps and warning Blueprint

Read spec UsageBudget, sections 6/8 and corresponding acceptance matrix. Use current helper interfaces from Tasks 1/3 and card optional fields from Task 2.

Files: new `budget.py`, `diagnostic_sensor.py`, root `blueprints/automation/codex_usage/usage_warning.yaml`; modify sensor/coordinator/card_data/translations, frontend parser/viewmodel only if contract integration requires; tests for budgets/timestamps/Blueprint and existing consumers.

- [ ] RED pure budget:18pp/72h=.25pph; finite0..100, known positive duration, future reset <=duration; zero preserved, no upper rate clamp. Core failure or age>max(900,2*configured interval) suppresses. New date/usage tuple recalculates atomically; no resetdate ->None. Canonical observation time calculated once shared by sensor/snapshot; card uses same value and calculated-at, not independent math.
- [ ] Add disabled-default no-state-class pp/h budget sensors for eligible windows with stable stems including canonical five-hour/weekly; dynamic recovery includes budget. Four disabled diagnostic timestamp sensors independent from core coordinator failure; unknown discovery stays unknown, enablement causes no HTTP.
- [ ] English Blueprint imports usage-percent sensor, threshold80, hysteresis5, dedicated restored input_boolean, user action selector. State/start/5min triggers; mode single; validate finite0..100/% and helperon/off. Warn only high+off; action then helperon; clear at max(0,threshold-hysteresis); unknown retains; no date/count rearm. No recipient/default external action.
- [ ] Add meaningful template/schema tests and actual HA runtime scenarios for later Linux gate:70,80,90,78,75,81 gives2 local calls; restart restoration, unknown, actionfailure retries, resetdate change high unchanged, low then high. Counting local service only.
- [ ] Run focused and full relevant suites, frontend build if changed. Commit `feat: add usage budgets and persistent warning blueprint`. Report remaining actual Linux validation requirements without marking them passed.

### Task 5: Complete documentation, runtime gates and 0.7 candidate

Read spec sections9-11; inspect final code and preceding reports rather than documenting planned behavior as implemented.

Files: README.md; new docs installation/card/entities/automations/troubleshooting/privacy/upgrading-to-0.7/release-notes-0.7/verification-0.7; real screenshots docs/images; `.github/workflows/validate.yml`, `tests_integration/`; version locations manifest, const CARD_VERSION/User-Agent, frontend package+lock and bundle; metadata tests.

- [ ] Short English README with actual rendered card and guides, retain/update existing anchors used by editor help. Complete entity table key/name/unit/class/default/source/unavailable. Explain visibility vs polling vs entity switches, credits vs money, signed values, shared usage and no per-app attribution, percentage-only admin settings, source memory/session scope and default-hidden diagnostics.
- [ ] Explain banked vs paid/automatic reset semantics with official source links already recorded; changing backend dates, temporary expiry reconciliation, no count inference, no redemption endpoint. Blueprint import/manual helper restore/crash gap/at-least-once retries and unobserved recoveries. Upgrade identity/history/metadata caveats, backup and rollback without destructive recorder edits.
- [ ] Add Linux runtime matrix pinned to compatible HA custom component test plugin for HA2026.3.0 and2026.8.3 (verify package metadata); cover setup/options/corefailure/Repairs/recorder metadata transitions/restored helper/real Blueprint execution. Do not force incompatible pins. Run locally if available; otherwise clearly record pending CI with exact reason.
- [ ] Actual rendered screenshots with synthetic accounts, English overview and details, narrow/wide and German visual verification. Keep secrets/provider identifiers out. Screenshot with browser skill and actual card; no fabricated mockup replacing implementation.
- [ ] Align all versions to0.7.0 only after features complete; generate committed bundle. Add metadata consistency test for every version locus. Prepare release notes locally.
- [ ] Run Ruff format/lint + Python full suite; frontend format/lint/typecheck/unit/coverage/audit/build and reproducible bundle; Playwright. Run available HA runtime gates; record unavailable HACS/hassfest/CodeQL or Linux checks as outstanding publication gates. Do not reuse baseline results.
- [ ] Whole-branch spec+quality review, fix actionable findings and rerun covering tests. Commit `docs: prepare and verify the 0.7 release candidate`. Final report lists implementation, actual checks, material pending gates, branch/commit/preview links. No release, merge, push or tag.
